const obj = {
    name : "Manisha",
    comp : "Mindtree"
}

console.log(Object.entries(obj));
// [ [ 'name', 'Manisha' ], [ 'comp', 'Mindtree' ] ]


/**
 * Object.entries api returns an array whose individual elements are the array of the individual key-value pair.
 */

