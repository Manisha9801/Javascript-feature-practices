const arr = [1,2,3]

console.log(arr);

const obj = {};
obj[0] = 1;
obj[1] = 2;
obj[2] = 3;

console.log(obj);

//Output of both will be same except that "length" property will only be present in arr not in obj.